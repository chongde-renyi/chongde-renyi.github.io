const search=document.querySelector('#search'),cards=[...document.querySelectorAll('#catalog article')],buttons=[...document.querySelectorAll('.filters button')],empty=document.querySelector('#empty');let filter='all';function update(){const q=search.value.trim().toLowerCase();let shown=0;cards.forEach(card=>{const okType=filter==='all'||card.dataset.type===filter,okSearch=!q||card.dataset.search.toLowerCase().includes(q)||card.innerText.toLowerCase().includes(q),show=okType&&okSearch;card.classList.toggle('hidden',!show);if(show)shown++});empty.classList.toggle('show',shown===0)}search.addEventListener('input',update);buttons.forEach(btn=>btn.addEventListener('click',()=>{filter=btn.dataset.filter;buttons.forEach(b=>b.classList.toggle('active',b===btn));update()}));;const coverCanvas=document.querySelector('#hemei-cover');if(coverCanvas){import('https://cdnjs.cloudflare.com/ajax/libs/pdf.js/4.10.38/pdf.min.mjs').then(async pdfjsLib=>{pdfjsLib.GlobalWorkerOptions.workerSrc='https://cdnjs.cloudflare.com/ajax/libs/pdf.js/4.10.38/pdf.worker.min.mjs';const doc=await pdfjsLib.getDocument('../books/hemei-50th/hemei-50th.pdf').promise,page=await doc.getPage(1),base=page.getViewport({scale:1}),scale=Math.min(2,Math.max(1,coverCanvas.parentElement.clientWidth/base.width*window.devicePixelRatio)),viewport=page.getViewport({scale});coverCanvas.width=viewport.width;coverCanvas.height=viewport.height;await page.render({canvasContext:coverCanvas.getContext('2d'),viewport}).promise;coverCanvas.classList.add('loaded')}).catch(()=>{})}
